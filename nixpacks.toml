[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
  "python manage.py collectstatic --noinput",
  "python manage.py migrate"
]

[start]
cmd = "gunicorn income_tax_project.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
